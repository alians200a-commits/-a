# فحص النسخة الأولى

- تمت معاينة صفحة إكمال الكلمات والتتبع في PDF الجديد، ومراجعة الكلمات الخمس وترتيب أجوبتها.
- فحوص Node للمنطق: رفض النقرة والقفز لنهاية مسار التتبع؛ قبول المسار المرتب والاستئناف؛ التحقق من كلمات المصدر؛ استعادة التخزين التالف؛ رفض علامة إكمال مزورة أو ناقصة؛ فتح المرحلة التالية بعد خمس كلمات مختلفة؛ عدم احتساب الإجابة الخاطئة أو تكرار الصحيحة.
- تحقق بنية JavaScript ومسارات ملفات CSS وJavaScript والصور المحلية.
- الصور المستخدمة أصول مستقلة؛ النص والأزرار عناصر HTML، وليست صورة شاشة ذات أزرار وهمية.
- تعذر فحص الواجهة في متصفح فعلي داخل بيئة التنفيذ: تنزيل Chromium فشل بسبب مهلة الشبكة/خطأ 502. لذلك لا ندعي اجتياز اختبار بصري أو اختبار لمس على هاتف؛ يلزم تجربة ذلك بعد تشغيل النسخة.
- الصورة الثابتة معتمدة لهذه المرحلة بتوجيه المستخدم. الفيديو مؤجل. الصوت غير مضاف.
- الاختبارات لا تثبت الصحة التربوية النهائية لمسار كتابة ى أو جودة التشكيل والنطق؛ تحتاج مراجعة قبل إصدار شامل.

التشغيل: `node --test tests/*.test.cjs`

## مراجعة 17 سبتمبر 2026

نجحت اختبارات المنطق الثمانية بعد تعديل الواجهة. أُضيفت خطوط محلية وcanvas-confetti. رفض متصفح المعاينة فتح مسار file المحلي بسياسة أمان؛ لم نحاول تجاوزها. لذلك لا يوجد فحص بصري فعلي أو قياس مؤكد لعدم التمرير على الهاتف.

## إصدار 0.3

نجحت 12 حالة اختبار: اختبارات المحتوى والحفظ والإجابات، واستجابة التتبع للعينات المتباعدة ورفض المؤشر الثابت والقفز على منحنى، وفحص HTTP لكل الملفات المعلنة ومنع خدمة الملفات غير العامة. نجح البناء إلى dist. هذه فحوص وظيفية ولا تثبت الشكل أو جودة اللمس: لم تُجر معاينة متصفح أو لمس فعلي، ولم يُتجاوز منع المتصفح السابق لفتح الملفات المحلية. العرض الإرشادي الجديد يحتاج معاينة عند التشغيل. خط العناوين Lalezar بديل مؤقت.

## إصدار 0.4 — توحيد الخط والتنسيق

- إعادة كتابة CSS واحد بعائلتين محليتين فقط: Cairo وLalezar؛ إزالة Noto وredesign.css من قائمة ملفات البناء.
- شريط الأنشطة والمتابعة منفصل عن منطقة تمرير المحتوى، والعداد معزول باتجاه LTR.
- motion.js يكيّف مؤثرات مرجع المستخدم ويحترم تقليل الحركة، مع حركة جمع الحرف وإبراز الكلمات. لا يتحرك مشهد الشخصيات الثابت.
- إصلاح تكرار مؤثر الإكمال عند تحريك المؤشر بعد انتهاء التتبع.
- 12 اختبارًا وظيفيًا ناجحًا بعد هذه التغييرات؛ syntax check لـapp.js وmotion.js والبناء ناجحان. لا معاينة متصفح أو اختبار لمس لهذه المراجعة؛ الصور التي أرسلها المستخدم توثق 0.3 فقط.
