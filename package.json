{
  "name": "qiraati-new-curriculum",
  "version": "0.6.0",
  "private": true,
  "description": "Iraqi grade-one new-curriculum alif lesson",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "node build.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": { "node": ">=18" }
}
